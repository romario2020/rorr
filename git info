git status
git branch # просмотр веток
git rm --cached <__> # исключить слежение
git add . # добавить все файлы
git commit -m "пиши коммит"
git branch test # создать ветку тест
git branch -D work # удалить ветку тест
git checkout master # переключение веток
git checkout -b new #создать ветку и переключиться на нее

git merge work #совмещаяем тест с мастером

git push # заливаем
git pull # выгребаем

git remote -v # в какая ссылка на обновление установленна

если сделать git checkout catalog/models.py
то файл откатится к состоянию как в гите
спокойно сделаешь pull
git diff catalog/models.py
покажет отличия
если ими можно пренебречь, то смело пиши checkout

откатить до состояния сервера и залить на хостинг
1. git checkout. (точка в конце)
2. git  pull
